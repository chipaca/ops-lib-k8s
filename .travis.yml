dist: bionic

language: python

python:
    - "3.5"
    - "3.6"
    - "3.7"
    - "3.8"

install:
    - pip install -r test-requirements.txt

script:
    - python -m black --line-length=99 --check --verbose k8s/ test/
    - python -m pytest -v

